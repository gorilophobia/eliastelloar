@font-face {
	font-family: 'Pavanam';
	src: url(fonts/Pavanam-Regular.ttf);
	font-style: normal;
	font-weight: 100;
}

@font-face {
	font-family: 'Eczar';
	src: url(fonts/Eczar-Regular.ttf);
	font-style: normal;
	font-weight: 100;
}

:root{
	--primer-color: #FFFFF0;
	--segundo-color: #130303;
	--tercer-color: #ffe866;
	--link-color: #153B50;
	--font-cuerpo: Pavanam;
	--font-titulo: 'Eczar';
}

h1, h2, h3, h4, h5, h6{
	font-family: var(--font-titulo);
	color: var(--link-color);
}

* { margin: 0px; padding: 0px ; 
  box-sizing: border-box ; 
}

body {
	position: relative;
	height: 100vh;
	font-family: var(--font-cuerpo);
   font-weight: 100;
   font-style: normal; 
	background-color: var(--primer-color);
	color: var(--segundo-color);
}

.el-papa{
	width: 100%;
	min-height: 95vh;
	padding: 2px;
}

#avatar{
	max-width: 100%;
	border-radius: 50%;
	margin: 0 auto;
}

/*---- Index ----*/


#index-container{
	max-width: 800px;
	min-height: 95vh;
	left: 0;
	right: 0;
	margin: auto;
	bottom:0;
	padding-top: 7.5vh;
}

#imagen-index{
	display: block;
	margin-left: auto;
	margin-right: auto;
	border-radius: 50%;
	max-width: 50%;
}	

#titulo-index{
	text-align: center;
	font-size: 3em;
	font-family: var(--font-titulo);
	color: var(--link-color);
	margin-top: 15px;
}

#container-lista-botones{
 	max-width: 50%;
 	margin-top: 2em;
 	margin-left: auto;
	margin-right: auto;
	text-align: center;
	max-width: 450px;
	padding: 5px;
}

#container-lista-botones * {
	display: inline-block;
}

.container-boton{
	text-decoration: none;
	font-family: Pavanam;
	color: black;
	font-weight: bold;
	text-align: center;
	font-size: 2em;
	
	background-color: var(--tercer-color);
	padding: 0.7rem 1.6rem 0.3em;
	border-radius: 0.5em;
	margin-bottom: 0.25rem;
	margin-right: 0.1rem;
	min-width: 50px;
	cursor: pointer;
	transition: all 0.1s;
}

.container-boton:hover{
	background-color: var(--link-color);
	color: var(--primer-color);
	font-weight: bold;
}

.info{
	font-size: 1.5em;
	font-family: var(--font-cuerpo);
	text-align: center;
}

/*---- Blog ----*/

#container-avatar{
	width: 200px;
	margin-top: 3vh;
	margin-left: auto;
	margin-right: auto;
}

#parrafo-bio{
	text-align: justify;
	max-width: 605px;
	padding: 5px;
	margin-left: auto;
	margin-right: auto;
	font-size: 18px;
	color: var(--segundo-color);
}

#container-lista-tags{
 	max-width: 50%;
 	margin-left: auto;
	margin-right: auto;
	text-align: center;
	max-width: 605px;
	padding: 5px;
}

#container-lista-tags * {
	display: inline-block;
}

.container-tag{
	background-color: var(--tercer-color);
	color: var(--segundo-color);
	padding: 0.15rem 0.3rem 0.1em;
	text-align: center;
	border-radius: 0.5em;
	margin-bottom: 0.25rem;
	margin-right: 0.1rem;
	font-size: 18px;
	min-width: 50px;
	line-height: normal;
	cursor: pointer;
	transition: all 0.1s;
}

.container-tag:hover{
	background-color: var(--link-color);
	color: var(--primer-color);
}

#container-lista-posts{
	max-width: 1000px;
	margin-left: auto;
	margin-right: auto;
}

#post-container{
	padding-top: 2px;
	padding-bottom: 2px;
	padding-left: 5px;
	padding-right: 5px;
	border-top: 1px solid var(--link-color);
	border-bottom: 1px solid var(--link-color);
}

#titulo-post{
	text-decoration: none;
	font-family: var(--font-titulo);
   font-weight: normal;
   font-style: normal;
	font-size: 32px;
	color: var(--link-color);
}

#titulo-post:hover{
	color: var(--segundo-color);
}

#descripcion-post{
	font-size: 18px;
	color: var(--segundo-color);
	margin-top: 2px;
	margin-bottom: 4px;
}

#lista-tags-post{
	max-width: 350px;
	margin-left: 0;
	margin-bottom: 0;
}

#lista-tags-post *{
	display: inline-block;
	font-size: 16px;
}

#fecha-post{
	text-align: center;
	font-size: 18px;
	opacity: 50%;
	float: right;
}

.paginador{
	position: relative;
	min-height: 1.5em;
	margin-top: 10px;
	margin-bottom: 10px;
	margin-left: auto;
	margin-right: auto;
}

.pag-elem{
	text-decoration: none;
	background-color: var(--segundo-color);
	color: var(--primer-color);
	padding: 0.15rem 0.3rem 0.1em;
	text-align: center;
	position: absolute;
	border-radius: 0.5em;
}

.pag-elem:hover{
	background-color: var(--link-color);
}

#nextero{	
	right: 0;
}

/*---- Página Single ----*/

.cuerpo-single{
	text-align: justify;
	color: var(--segundo-color);
	font-size: 24px;
	line-height: 1.75;
	max-width: 1010px;
	padding-right: 10px;
	padding-left: 10px;
	margin-left: auto;
	margin-right: auto;
	margin-bottom: 5px;
}

.footnotes{
	padding: 1em;
	text-align: justify;
	font-size: 16px;
	line-height: 1.5;
}

.footnote-ref{
	text-decoration: none;
	font-size: 0.75em;
	font-weight: bold;
	color: var(--link-color);
	padding: 0 3px 0 3px;
	border: 1px solid var(--link-color);
	border-radius: 100%;
	line-height: 1;
}

/*---- Página List ----*/

.titulo-general{
	text-align: center;
	font-size: 42px;
	font-family: var(--font-titulo);
	color: var(--link-color);
}

/*---- Footer ----*/

footer {
	width: 100%;
	text-align: center;
	align-self: flex-end;
	margin-bottom: 1em;
}

.footnote-backref{
	text-decoration: none;
	font-weight: bold;
	color: var(--link-color);
}

footer * {
	text-decoration: none;
	color: var(--link-color);
	font-weight: bold;
	font-size: 20px;
	opacity: 70%;
}